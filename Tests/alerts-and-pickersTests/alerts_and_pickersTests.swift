import XCTest
@testable import alerts_and_pickers

final class alerts_and_pickersTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(alerts_and_pickers().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
